import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
	textScore: {fontSize: 15, color: 'white'},
	textYellowScore: {fontSize: 25, color: 'yellow'},
	raitingContainer: {
		alignItems: 'center',
		borderRadius: 10,
		padding: 5,
	},
});
